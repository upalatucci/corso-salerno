<div class="aurora-bg" aria-hidden="true"></div>

<style is:global>
  .aurora-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background:
      radial-gradient(ellipse 80% 60% at 15% 10%, var(--rosa) 0%, transparent 60%),
      radial-gradient(ellipse 70% 50% at 85% 5%, var(--lavanda) 0%, transparent 55%),
      radial-gradient(ellipse 60% 70% at 5% 80%, var(--pesca) 0%, transparent 55%),
      radial-gradient(ellipse 75% 55% at 90% 85%, var(--lilla) 0%, transparent 55%),
      linear-gradient(160deg, #f5e6fb 0%, #fbe8f5 40%, #f0e8fc 70%, #fde8e0 100%);
  }
</style>
